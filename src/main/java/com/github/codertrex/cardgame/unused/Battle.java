package com.github.codertrex.cardgame.unused;
public class Battle {
    int characterDefense = 0;
    int characterAttack =0;
    int monsterDefense = 0;
/*
    public void fight (Character character, Monster monster){
        monster.intoText();
        int monsterHealth = monster.getHealth();
    while(character.getHealth() > 0 && monsterHealth > 0){
        getCard();
        monsterHealth -= (characterAttack - monsterDefense);
    }
    }
    public void getCard(DefenseCard card){
        characterDefense += card.getDefence();
    }

    public void getCard(AttackCard card){
        characterAttack += card.getDamage();
    }

 */
}

